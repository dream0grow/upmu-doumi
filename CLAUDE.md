# CLAUDE.md — 얇은 라우팅

이 프로젝트는 **교무부장 도우미**입니다. 학교 공문을 100% 로컬에서 처리합니다.

## 절대 원칙 (설계서 확정 + 2026-07 개발자 재정의)
1. **개인정보 로컬 보호** — 로컬 100%의 핵심 목표는 **학생·교사 개인정보가
   외부로 유출되지 않는 것**. 공문 원문·개인정보는 PC 밖으로 절대 안 나가고,
   요약·추출 등 공문 처리는 전부 로컬(규칙 + Ollama)에서만.
   단, **개인정보가 아닌 사용 피드백·분류 수정 내역(공문 제목 수준)**은
   사용자가 내용을 눈으로 확인하고 '보내기'를 눌렀을 때만 관리자에게 전송 가능.
2. **정확한 사실은 규칙으로, 창의적 판단은 AI로** — 마감일·발신기관·문서번호는 코드 추출, 요약·할일은 AI 제안.
3. **학생·교사 식별정보 절대 비저장.**
4. **최소 코드** — 과한 추상화 금지. 한 기능씩 완성.

## 개발 순서
- [x] **MVP-1** 파일에서 텍스트 추출 (5형식 + 파일명 파싱)
- [x] **MVP-2** 마감일 규칙 추출 + 라벨 구분(신청기한/제출기한/행사일)
- [x] **MVP-3** 교무수첩 항목 생성 (제목·발신·마감·성격·D-day 자동)
- [x] **MVP-4** AI 요약·할일 (Ollama 연동, 근거표시 프롬프트)
- [x] **MVP-5** 홈 대시보드 — main 병합 완료 (PR #19), 개발자 실기기 검증 대기
- [ ] MVP-6 교무수첩 화면 — **코드 완료** (아래 현재 상태 참고)

## 현재 상태 (2026-07-06, 인수인계)
이전 인수인계분(홈 대시보드 개편·공문 집어넣기·의견 보내기·공문 세트 묶기·투두리스트)은
**전부 main 병합 완료** (PR #19). 레포 분리도 끝남(100study 이전, 로켓스타트 제거).
상세 내역은 PR #19 와 `docs/교무수첩규칙.md`·`docs/AI규칙.md` 참고.

**작업 브랜치: `claude/claude-md-handoff-review-wsro8x`** — 📒 교무수첩 화면 신설 (빌드가이드 §3-3):
- 할일형 공문 우선순위 목록 — **행 드래그로 부장이 직접 순서 지정**
  (`cards.note_order` 컬럼, 스키마 v4. 마이그레이션은 컬럼만 추가 — 기존 카드 보존).
  순서를 안 정한 카드는 마감 가까운 순 자동 정렬 (우선순위 자동화 안 함 = 설계 원칙)
- 카드마다 **세부 할일 체크박스** — 홈 투두리스트와 같은 저장소(`todos.card_id`)라
  어느 화면에서 체크해도 같이 갱신
- 공람형(담임·교사 안내) 체크 목록 + 카드 상세 모달 공용
- 파일: `app/renderer/src/pages/Notebook.tsx` 신설, db/main/preload/types 에 note_order 연결
- UI 문구는 윤문 스킬(`/im-not-strange-ai`) 통과 (12건 중 1건 수정)

바로 다음 할 일:
1. **개발자가 구글 폼(단답형 3문항: 보낸이/의견/데이터)을 만들어 링크를 주면**
   → 폼 HTML 에서 entry ID 추출해 `feedback_config.json` 채우고 커밋 (계속 대기 중)
2. 개발자 실기기(맥/윈도우) 검증 — 홈 대시보드 + 교무수첩 함께
3. 이 브랜치 main 병합 여부 개발자에게 확인 (검증 통과 후)
4. 이후 화면: 공문 작성(RAG) → 회의록 순 (빌드가이드 §3)

## 코드 위치
- `src/parsers/` — 형식별 본문 파서 (pdf·hwp·hwpx·odt·xlsx·xls)
- `src/extractor/` — 규칙 기반 추출: `filename_parser`(파일명), `deadline_parser`(마감일), `notebook`(교무수첩 카드)
- `src/ai/` — 로컬 LLM(Ollama) 연동: `prompt`(근거표시), `ollama_client`, `summarizer`(요약·할일)
- `src/extract.py` — 텍스트·마감일·교무수첩·AI 총괄 + 명령줄 입구
- `docs/파싱규칙.md`·`docs/마감일규칙.md`·`docs/교무수첩규칙.md`·`docs/AI규칙.md` — 규칙 상세

## 세부 문서
- 파싱: `docs/파싱규칙.md`
- 마감일: `docs/마감일규칙.md`
- 교무수첩: `docs/교무수첩규칙.md`
- AI(요약·할일): `docs/AI규칙.md`
- 실행법·설치: `README.md`

> 개발자는 현직 교사이며 코딩 학습 중입니다. 코드는 친절한 한국어 주석과 함께 작성합니다.
> 설계 결정을 바꿔야 하면 먼저 개발자에게 확인하세요.

## 한국어 윤문 스킬 — 제3자 노출 문구는 무조건 적용 (필수)

제3자에게 보여주는 **모든 한국어 문구**를 작성·수정했다면, 발행·커밋 전에 반드시 아래 윤문 스킬을 거친다.
대상 예시: 스레드 글, 카드뉴스, 뉴스레터, 영상 스크립트, 강의 스크립트, 홈페이지·랜딩 카피라이팅,
앱 화면 텍스트(버튼·안내·오류 메시지 등 UI 문구), 상세페이지, SNS 게시물.
제외: 코드 주석, 내부 문서, 커밋 메시지 등 사용자 본인만 보는 텍스트.

- **기본**: `/im-not-strange-ai` — Sunny 문장 규칙 포함 (`.claude/skills/im-not-strange-ai`)
- **대안**: `/humanize-korean` 또는 `/humanize` (`.claude/skills/humanize-korean`)
- 8,000자 초과·정밀 검증이 필요하면 `--strict` (5인 파이프라인)
- 원칙: 의미·사실·수치는 한 글자도 바꾸지 않고 문체·리듬·표현만 다듬는다

스킬·에이전트는 이 저장소 `.claude/skills/`·`.claude/agents/`에 동봉되어 있다.
출처: https://github.com/epoko77-ai/im-not-ai · https://github.com/itssosunny/im-not-strange-ai
