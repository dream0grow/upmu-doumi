# 교무부장 도우미 — 파일 텍스트 추출(MVP-1)에 필요한 라이브러리
# 설계서 "3. 파일 파싱(검증 완료)"에서 확정된 것들만 담았습니다.

pdfplumber==0.11.4   # PDF(텍스트) 추출
openpyxl==3.1.5      # XLSX(엑셀) 표 읽기
olefile==0.47        # HWP(구형 5.0) 안의 저장 구조 열기
# HWPX / ODT 는 파이썬 표준 라이브러리(zipfile, xml)만으로 처리 → 추가 설치 불필요
